# 快速开始指南

## 系统要求

- **操作系统**: Windows 10/11 (64位)
- **显存**: 8GB+ (推荐12GB)
- **内存**: 16GB+ (推荐32GB)
- **硬盘**: 10GB 可用空间
- **Python**: 3.10 或 3.11 (**不要用 3.14**)

---

## 第一步：安装基础环境

### 1. 安装 Python 3.11

**重要：不要安装 Python 3.14（预览版），会导致很多包无法安装！**

下载地址（华为云镜像，国内快速）：
```
https://mirrors.huaweicloud.com/python/3.11.9/python-3.11.9-amd64.exe
```

**安装时必须勾选 "Add Python to PATH"**

### 2. 安装 Node.js

下载地址（华为云镜像）：
```
https://mirrors.huaweicloud.com/nodejs/v20.18.0/node-v20.18.0-x64.msi
```

### 3. 验证安装

安装完成后**重启电脑**，然后打开命令提示符(CMD)，运行：
```cmd
python --version
node --version
```

应该显示版本号，如 `Python 3.11.9` 和 `v20.18.0`

---

## 第二步：下载 IndexTTS

由于不需要 Git，请手动下载：

1. 访问：https://github.com/index-tts/index-tts
2. 点击绿色 **Code** 按钮 → **Download ZIP**
3. 解压到桌面（解压后文件夹名应为 `index-tts-main`）

---

## 第三步：运行安装脚本

双击运行：
```
scripts\install.bat
```

等待安装完成（约 5-10 分钟）

如果 IndexTTS 没有自动安装，运行：
```
scripts\install_indextts.bat
```

---

## 第四步：下载模型文件

双击运行：
```
scripts\download_model.bat
```

或手动下载：
1. 访问：https://hf-mirror.com/IndexTeam/Index-TTS/tree/main
2. 下载所有文件（约 3GB）
3. 放入 `backend\checkpoints\` 目录

需要的文件：
- `gpt.pth` (主模型)
- `bigvgan_generator.pth`
- `bigvgan_discriminator.pth`
- `dvae.pth`
- `bpe.model`
- `config.yaml`
- `unigram_12000.vocab`

---

## 第五步：启动应用

双击运行：
```
scripts\start.bat
```

浏览器访问：http://localhost:1420

---

## 常见问题

### Q: Python not found?
重新安装 Python 3.11，确保勾选 "Add Python to PATH"，然后**重启电脑**。

### Q: numpy 编译错误？
你安装了 Python 3.14（预览版）。请卸载后安装 Python 3.11。

### Q: 模型加载失败 "No module named 'indextts'"？
运行 `scripts\install_indextts.bat`

### Q: 模型加载失败 "is_half" 错误？
删除 `backend\modules\__pycache__` 文件夹后重启

### Q: 模型下载太慢？
使用迅雷等工具从 https://hf-mirror.com/IndexTeam/Index-TTS/tree/main 下载

---

## 脚本说明

| 脚本 | 功能 |
|------|------|
| `scripts\install.bat` | 一键安装所有依赖 |
| `scripts\install_indextts.bat` | 单独安装 IndexTTS |
| `scripts\download_model.bat` | 下载模型文件 |
| `scripts\start.bat` | 启动应用 |
| `scripts\fix_missing_packages.bat` | 修复缺失的包 |

---

## 完整安装流程

```
1. 安装 Python 3.11（勾选 Add to PATH）
2. 安装 Node.js  
3. 重启电脑
4. 下载 IndexTTS ZIP 并解压到桌面
5. 双击 install.bat（等待完成）
6. 双击 install_indextts.bat（如果第5步没装上）
7. 双击 download_model.bat（下载模型）
8. 双击 start.bat（启动应用）
9. 浏览器访问 http://localhost:1420
```
